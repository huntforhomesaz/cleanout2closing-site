---
/**
 * src/pages/az/tucson/estate-cleanout-and-home-sale.astro
 * Route: /az/tucson/estate-cleanout-and-home-sale
 * Content source: content/geo/az-tucson-estate-cleanout-and-home-sale.md
 * Layout: GeoServiceLayout
 * Task 7.2.3 - Content wiring complete
 */

import { getEntry, render } from 'astro:content';
import GeoServiceLayout from '../../../layouts/GeoServiceLayout.astro';

// Load content from collection
const entry = await getEntry('geo', 'az-tucson-estate-cleanout-and-home-sale');

if (!entry) {
  throw new Error('Content not found at content/geo/az-tucson-estate-cleanout-and-home-sale.md');
}

const { Content } = await render(entry);
const frontmatter = entry.data;
---

<GeoServiceLayout frontmatter={frontmatter}>
  <Content />
</GeoServiceLayout>
