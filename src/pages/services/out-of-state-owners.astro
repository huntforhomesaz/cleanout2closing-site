---
/**
 * src/pages/services/out-of-state-owners.astro
 * Route: /services/out-of-state-owners
 * Content source: content/pages/service-out-of-state-owners.md
 * Layout: ServicePillarLayout
 * Task 7.2.3 - Content wiring complete
 */

import { getEntry, render } from 'astro:content';
import ServicePillarLayout from '../../layouts/ServicePillarLayout.astro';

// Load content from collection
const entry = await getEntry('pages', 'service-out-of-state-owners');

if (!entry) {
  throw new Error('Content not found at content/pages/service-out-of-state-owners.md');
}

const { Content } = await render(entry);
const frontmatter = entry.data;
---

<ServicePillarLayout frontmatter={frontmatter}>
  <Content />
</ServicePillarLayout>
